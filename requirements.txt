setuptools>=65.0.0
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
# visual_ai.display's portable backend. Hard dependency: without it the only
# window a non-Windows machine can open is the null one.
pygame>=2.5.0
pybind11>=2.11.0
psutil>=5.9.0
gputil>=1.4.0
